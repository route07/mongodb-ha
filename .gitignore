# TLS Certificates (sensitive)
tls-certs/
*.key
*.pem
*.csr
*.srl

# Database data
db_data/
# Replica set data directories (HA setup)
db_data_primary/
db_data_secondary*/

# Export files
*_export_*.json

# Node modules (only in subdirectories if used)
database-export/node_modules/
mongo-admin/node_modules/
ipfs-backup/node_modules/
dumps/
*.zip

# Environment files
.env
.env.local

agent.sh
